{
  "version": 2,
  "builds": [
    {
      "src": "packages/app/vite.config.ts",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "packages/app/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/packages/app/$1"
    }
  ]
}
